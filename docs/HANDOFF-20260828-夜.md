# 苏苏洛桌宠 · 会话交接（2026-08-28 深夜）

> 写给任何接手者（模型/开发者）：本卡自包含，无需翻聊天记录即可继续。
> 配套：`docs/optimization-progress.md`（账本，最新 §14 追加 104）、`docs/NEXT-ACTIONS-20260827.md`、
> `docs/SECURITY-TEST-EXEC-20260827.md`（安全测试双遍表，已完结）、`deploy/README.md`（打包 SOP）。

---

## 0. 一句话现状

安全测试两遍完结；S05 已 by asar 打包根治（**工作流已变：更新必须重打包**）；维什戴尔两套皮肤已下载待验收；
用户提出"默认装显示偏小"要调 + "绝对主角"第二套时装要补下——见 §3 待办。

## 1. 硬事实

| 项 | 值 |
|---|---|
| dev | `E:\SuzuranPetGit`；prod | `E:\SuzuranPetGit\release\v2.5\苏苏洛桌宠 2.5 正式版` |
| **运行形态** | **resources/app.asar（173M）**；散目录已改名 `app_legacy`（回退=删 app.asar+改名回 app） |
| 皮肤 | userData `%APPDATA%\苏苏洛桌宠 1.1 正式版\assets\spine\user\`（懒加载，重启见托盘皮肤菜单） |
| 密钥 | userData `secrets.v1.json`（DPAPI），生效配置 = userData `config.json` |
| 日志 | userData `logs\tts.log`（UTC）；Agent `http://127.0.0.1:8765`（/health、/chat） |
| 单测 | 17 套全绿（tests/） |
| VM | `SuzuranPet-TestVM` **powered off（瘦身完结待命）**；快照已合并删除；下次 `startvm --type headless` 即用；gues 已禁 wuauserv |
| 代理 | 宿主 Clash `http://127.0.0.1:7897`（下载/GitHub raw 都要 -x 它；raw 对含 `#` 路径要 `%23` 且偶发 404 重试可通） |

## 2. 【重要】更新部署 SOP 已变（asar 时代）

```bash
# 1) dev → prod 资源目录（散）
for f in main.js src/xxx.js ...; do cp -f "/e/SuzuranPetGit/$f" "$PROD/resources/app/$f"; done
# 2) 重打 asar（散目录直改不再生效！）
bash deploy/pack.sh          # 内部：mv app→app_legacy → npx asar pack → app.asar.new→app.asar
# 3) 语法/单测先行：node --check + 17 套测试
# 4) 重启 + 验证（5 进程 / health / [security] / 冒烟聊天【中文 body 必须 --data-binary @UTF-8文件】）
```

## 3. 待办（按优先级）

1. ~~下载维什戴尔第三套「成就之星/IX」~~ **✅ 已下齐三套**：`1035_wisdel`（默认）、`1035_wisdel_sale_14`（忒斯特收藏/XV）、`1035_wisdel_game_9`（成就之星/IX，即用户说的"绝对主角"对应时装）——ALL 在 userData assets/spine/user/，`#`→`_` 已处理。
2. ~~维什戴尔默认装显示偏小~~ **✅ 已调到位**：`SKIN_SCALE_OVERRIDE = { "1035_wisdel": 1.5 }`（并入 boost、dirName 作用域内应用）；`final=0.2167 w=162 h=91`（与 sale_14 的 91px 观感对齐，用户确认其他两套完美）。**坐姿自动缩小已修**：守卫宽度约束放宽 12% 余量（坐姿 Relax 包围盒 125>画布 120 致 k≈0.9 缩小；已确认修复）。若日后仍需微调：改该表数值 → asar 流程重打。
3. 等用户验收（明早）：维什戴尔三套皮肤托盘菜单选择、默认装放大效果。
4. 3D 渲染模式：**已整体移除（2026-08-28 用户放弃）**——代码/资源/打包参数/工作文件全清，账本 §106 保留技术手册（如需再启用照 §106 即可）。
5. 平时更新代码按 §2 asar 流程；账本追加编号顺延（当前 105）。

> 注：asar 流程新坑已修——`deploy/pack.sh` 已支持"仅 app_legacy（上次打包源）存在时自动提权为 app"；打包前**必须先停应用**（app.asar 被占用会改名失败）。

## 4. 红线与习惯

- 看图/截图**必须走 vision skill（vision.js）**，禁止直接 Read 图片。
- 中文 body 一律文件体（curl `--data-binary @文件`），直接 `-d '中文'` 会 GBK 乱码污染上下文。
- VM 测试只动 VM；截成本/动生产先备份（`E:\vmwork\archive\` 有 asar 前散目录备份）。
- 皮肤 `#` 坑：目录/文件名与 atlas 内贴图引用都要 `#`→`_`。

## 5. 快捷指令

```bash
PROD="E:/SuzuranPetGit/release/v2.5/苏苏洛桌宠 2.5 正式版"
# 重启应用（Git Bash）
powershell -NoProfile -Command 'Get-Process | Where-Object { $_.ProcessName -like "*苏苏*" } | Stop-Process -Force; Start-Sleep -Seconds 3'
cd "$PROD" && cmd //c start "" "苏苏洛桌宠 1.1 正式版.exe"
# 代理下载范例（含 # 皮肤）
curl -s -L -m 300 -x http://127.0.0.1:7897 "https://raw.githubusercontent.com/isHarryh/Ark-Models/main/models/<id含%23>/build_char_<id含%23>.skel" -o out.skel
# VM
VBM="C:/Program Files/Oracle/VirtualBox/VBoxManage.exe"; "$VBM" startvm SuzuranPet-TestVM --type headless
```