# 苏苏洛桌宠 · 会话交接（2026-08-29 凌晨）

> 自包含：接手者无需翻历史。当前桌面宠处于**稳定 Spine 运行态**；进行中的是 **PSD 耳朵分离任务**。
> 配套：账本 `docs/optimization-progress.md`（最新 §108：rig 渲染管线治本）、`deploy/README.md`（打包 SOP，本轮已撤销未执行）、`docs/HANDOFF-20260828-夜.md`（已被本文件取代）。

## 0. 一句话现状

昨晚一系列工作（安全测试、S05 asar 打包根治、维什戴尔皮肤三套、记忆编辑、World Info、向量记忆、3D 试水→整体移除、打包流程撤销）全部落定；桌宠运行正常（Spine + 冬季苏苏洛）。**PSD 耳朵分离任务已完整体验通过（§108）：2.5D 皮肤现在有耳朵、无错位放大、脸完整。当前 rig 皮肤为 `seethrough_output_faceears.psd`（faceears3 修正版）。**

## 1. 硬事实（沿用）

| 项 | 值 |
|---|---|
| dev | `E:\SuzuranPetGit`；prod | `E:\SuzuranPetGit\release\v2.5\苏苏洛桌宠 2.5 正式版` |
| 运行形态 | **resources/app.asar**（173M，散目录 `app_legacy` 作打包源；pack.sh 一键重打） |
| 皮肤(Spine) | userData `assets\spine\user\`（维什戴尔三套已就位，「1035_wisdel」（默认，override ×1.5）、sale_14、game_9） |
| PSD(2.5D) | userData `assets\rig\user\*.psd`（rigSkinId 由 config 指定） |
| 生效配置 | userData `config.json`（生产根 config 是首启模板，**已从备份还原为用户原样**，勿再换成出厂模板除非正式发版） |
| 密钥/日志/Agent | secrets.v1.json（DPAPI）/ `logs\tts.log`（UTC）/ `127.0.0.1:8765`（/health、/chat） |
| 单测 | 17 套全绿；部署处处 **先停应用→改→pack.sh→启动**（asar 占用与配置竞争教训） |
| VM | 关机待命（`startvm --type headless` 即用；已禁 wuauserv） |
| 代理 | `http://127.0.0.1:7897`（下载/raw 用；GitHub raw 对含 `#` 用 `%23`） |

## 2. PSD 耳朵分离（已完成并验收：§108 四修复）

**过程**：`seethrough_output.psd`（rigUser）"front hair"图层两侧画着兽耳，rig 显示不出现。从 front hair 裁出独立 `headwear` 层 → 多版迭代（ears / ears_v2 放大描边 / faceears 烘入 face 全失败）→ **最终定位四个根因并治本**（详见 optimization-progress §108）：
1. cancelAnimationFrame 误杀渲染循环（角色整体消失）——删除；"0.73s 心跳"是 165Hz 显示器正常频率，非多实例。
2. straight-alpha 图按 premultiplied 上传（透明像素 RGB 残留渲染成色块）——上传前真正预乘。
3. faceears 的 front hair 被误挖脸中央横带（露灰色 back hair = "错位放大"）+ face 被烘入弄脏——front hair/face 整体用原始 PSD 版替换；headwear 清掉顶部残留圆弧块。
4. canvas 非等比拉伸（object-fit 对 canvas 无效）——rig-runtime 按窗口高 88% 等比算 canvas 尺寸（__fitCanvas）+ resize 跟随。

**当前产物**：`seethrough_output_faceears.psd` = faceears3 修正版（= faceears 骨架 + 原始 front hair/face + 清理版 headwear 两耳）。原版备份 `seethrough_output_faceears.psd.bak-20260828`。ears.psd / ears_v2.psd 因 front hair 挖洞作废（要复用需同样修 front hair）。

**验证结论**：静态合成（Canvas2D z 序 + fit 复刻）无灰条、耳朵对称可见、脸完整；运行时与静态 RGB 对比 89.9% 吻合。"灰色横条"经像素扫描证实为 vision 幻觉，实际不存在。

**验收路径**：设置 → 渲染模式 → PSD 2.5D → 皮肤 `seethrough_output_faceears` → 应看到头两侧兽耳、脸完整、无变形。rig 皮肤列表动态读 rigUser，切换无需重启。

**rig 渲染诊断**：rig-runtime.js 带 `[rigdbg]` 日志（script loaded / applyRig done / tick alive / NaN / frame whole+headwear bbox 每 600 帧）。诊断工具在 `outputs/`：cap-rig.ps1（PrintWindow 抓窗，UTF-8 BOM 必须）、render-zorder.js（z 序静态合成复刻）、find-char-bbox.js 等。

## 3. 规则与命令速查

- 部署三步：`node --check` + 17 套单测 → 复制到 `resources\app_legacy\` → `bash deploy\pack.sh` → 重启 5 进程验证。
- 聊天/测试中文 body 一律 `--data-binary @UTF8文件`（curl -d 中文=GBK 乱码坑）。
- 备份总库：`E:\vmwork\archive\`（含 20260828-asar-stable 完整回退件）。
- 正式打包发布（**用户暂缓，改完 PSD 后再定**）：deploy/README §1（同步→出厂 config 替换→pack.sh→zip），已撤销未执行。
- 图片判读红线：一律走 `vision skill`（`node C:\Users\xsbil\.zcode\skills\vision\vision.js <png> "提问"`），禁止 Read 直读；但 **vision 描述不可尽信**（多次幻觉"灰色横条"），关键结论必须配像素级脚本验证（bbox 扫描 / RGB 对比 / 图层归属统计）。