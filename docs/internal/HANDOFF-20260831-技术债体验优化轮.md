# 交接文档 · 技术债与体验优化轮（2026-08-30 ~ 08-31）

> **写给下一个接手的模型/开发者**。本文档自包含：本轮全部改动、发布物状态、已知问题、待办、工作规矩。
> 前序交接见 `docs/HANDOFF-20260829-总交接.md`（v2.5 收尾）。逐项修复细节账本见
> `docs/MAINT-20260830-H1H2.md`（本轮的实时维护记录，中途断了读它可恢复）。
>
> **工作规矩（用户明确要求，必须遵守）**：
> ① 改任何文件前先确认 git 干净（备份点）；② 调试好、用户验收后才 push；③ 小步验证：改 dev → 同步
> `正式版\resources\app_legacy\` → 停桌宠 → `bash deploy/pack.sh` → 重启实测；
> ④ **Mimosa hook 红线**：禁止 Bash 直接写源码/配置（必须用 Write/Edit 工具）、commit 要用
> `git -C "E:/SuzuranPetGit"`（否则 hook 误扫 workspace 目录报无关高危）；⑤ 图片判读必须走 vision skill；
> ⑥ 中文 curl body 一律 `--data-binary @UTF8文件`；⑦ **代理已挂**：push 用
> `git -C ... -c http.proxy= -c https.proxy= push origin main`（直连 GitHub 可用）；⑧ 找桌宠进程用
> PowerShell `Get-CimInstance Win32_Process`（Git Bash grep 中文进程名会失效）。

---

## 一、一句话现状

**v2.5.17 全部完成并部署**：git main = `8d817b2`（全部已 push，工作区干净）、asar 为最新
185,830,531 B（已打包并重启验证）、用户桌宠运行中（6 进程）、日语模式省内存生效（Genie 未拉起）。
**无未完成的关键修复**。剩余待办：H3（Agent client token 加密）、完整版 zip 的 engines 未含 Genie
修复、朋友 B 需要确认（见第五节）。

## 二、本轮改动清单（8 个 commit，全部已 push + 进包）

| commit | 内容 |
|---|---|
| `3c75075` | **台词库扩容 + 防连续重复 v2.5.14**：各池约翻倍（摸头 7→14、工作流 6→14、时段 10-12→17-19、闲置 8→14、互动各 4→8、入睡各 3→6、清晨 2→5、阶段各 2→4）；`pick()` 记住近 3 条不重复（WeakMap），连抽 50 次无连续重复实测通过 |
| `495c9e7` | **技术债 H1+H2**：H1=features.js 补 `startSystemMonitor`/`stopSystemMonitor`（main.js 两处调用但函数缺失 → 勾选"系统监控播报"后下次启动 TypeError 中断初始化）；H2=仓库 config.json + app_legacy config.json 的 `agentApi.enabled: true→false`（新用户不再默认开放零鉴权 8765 端口）；本机 live config 未动 |
| `0ed0700` | **时间感知 v2.5.16**：`petStateNote()` 注入具体时:分（`下午（15:32）`）——被问"现在几点"不再编造；台词去掉写死时刻（"下午三点"→"午后到傍晚"） |
| `58c661b` | **词库时间合理性全量审查 + 剪贴板 3 bug**：morning/evening 池边界穿帮 4 处改中性表述；wake 池整体去"早安"（任意时刻睡醒会触发）；剪贴板 `readText()` 补 try/catch、URL 正则支持裸域名、数字判断收紧为整段数字 |
| `7f66611` → `ee4b0b2` | **许可变更（最终 BUSL-1.1）**：历程=自造"禁止商用"许可（违反 OSI 开源定义第 6 条无领域歧视）→ MIT → **用户最终选定 BUSL-1.1**（源代码可得、禁止商用、2030-08-30 自动转 MIT）。LICENSE/TERMS/TERMS-TEMPLATE/terms.html 弹窗/package.json 全部同步；**不再自称"开源"，改称"源代码可得"** |
| `0935a50` | **xlsx 0.18.5 → 0.20.3（安全）**：CVE-2023-30533 原型污染 + CVE-2024-22363/23344 ReDoS。官方 CDN 安装（npm 不再更新社区版），dev/app_legacy 的 node_modules 同步替换（旧版备份 `_backups/xlsx-0.18.5-before-upgrade`），API 兼容验证通过 |
| `8d817b2` | **日语模式省内存 v2.5.17**：speakJa 开启时跳过/停止 Genie 中文引擎（省 ~4.7G，只留 GSV 日语引擎）；启动预热/开关/语音开关三处联动；日语链路失败一律静音不回退中文（4 分支：翻译失败/无可用句/服务不可用/冷却中） |

## 三、关键路径与环境

| 项 | 值 |
|---|---|
| dev | `E:\SuzuranPetGit`（git main = 8d817b2，全部已 push，工作区干净） |
| 正式版 | `E:\SuzuranPetGit\release\v2.5\苏苏洛桌宠 2.5 正式版\`（用户日常运行中，6 进程） |
| 部署循环 | 改 dev → **用 Write/Edit** 同步到 `正式版\resources\app_legacy\` → 停桌宠 → `bash deploy/pack.sh` → 重启 exe |
| asar 占用 | pack.sh 常报 `Device or resource busy`（残留句柄）→ **用 `cp -f app.asar.new app.asar` 覆盖**（rename 失败但 cp 有效） |
| 用户数据 | `E:\SuzuranPetData\`（**C 盘原路径是 junction 指向它**，勿破坏链接） |
| 引擎源（勿删） | `E:\GenieTTS`（中文 Genie）、`E:\GSV-training`（日语 GPT-SoVITS）、`E:\SussurroTrain`（训练素材） |
| 语音链路 | 中文=Genie(9881，~4.7G)；日语=翻译→GPT-SoVITS(9880，~2G，GSV)；日语模式 Genie 不拉起 |
| 日志 | `E:\SuzuranPetData\logs\tts.log`（UTC 时间戳） |
| Agent | `http://127.0.0.1:8765`（/health、/chat、/stop、/status；master+client token） |
| 维护账本 | `docs/MAINT-20260830-H1H2.md`（实时记录，含给接手模型的提示） |

## 四、环境变化（本机，08-30 顺手做的，与代码无关但要知悉）

1. **C 盘迁移**：`%APPDATA%\SuzuranPet`（39M 主数据）→ 已迁移到 `E:\SuzuranPetData`，原路径是 **junction 链接**（应用无感知，升级/重打包不受影响）。**不要删除或重建该链接**；若需要访问数据直接走 E 盘路径。
2. **C 盘清理**：删了旧版 `%APPDATA%\suzuran-pet`（3M Chromium 缓存）+ `%APPDATA%\Local\electron\Cache`（378M dev 缓存）+ `pip cache purge`（6G）——共释放 ~6.4G。
3. **C 盘大头另有其主**：NVIDIA 驱动缓存（17G）、剪映（9.5G）、pip（已清）等，与项目无关。
4. **代理挂了**：127.0.0.1:7897 连接失败（netstat 全是 FIN_WAIT_2，服务疑似死）。GitHub 直连正常。git 操作记得 `-c http.proxy= -c https.proxy=` 绕过。

## 五、待办与已知问题（按优先级）

1. **H3（技术债，用户暂缓）**：Agent client token 明文落盘 config.json（主 token 走 DPAPI 加密，client token 未加密，安全模型不一致）。落地要点见 `MAINT-20260830-H1H2.md` 末尾：client token 移入 secrets.js（DPAPI），config.json 只存指纹。**用户明确说"先待定"，做之前先问。**
2. **完整版发布 zip 未含 Genie 修复**：`engines/genie/genie_tts_server.py`（mkstemp 竞态修复）+ `ToneSandhi.py`（G2P 空串崩溃修复）改的是 `E:\GenieTTS` 和随包 engines，但**完整版 zip 未基于这些修复重新打包**。朋友 B 要拿最新完整版需重新发 zip（约 16GB staging，耗时）或单发 2 个 engine 文件 + 新 asar。
3. **朋友 B（NaCl_Yan）交付**：本轮全部修复都在 asar 里，**替换 `app.asar` 重启即可**。上轮 Genie 修复（engine 2 文件）确认过是否已给。若朋友反馈"日语模式没声音"——检查是否已开启 speakJa 且 GSV(9880) 可达（失败现在会静音，tts.log 有"静音（日语模式不退回中文）"字样可定位）。
4. **已知限制（非本轮引入）**：
   - 设置页"剪贴板感知/系统监控"开关保存后**需重启才生效**（save-settings 无即时启停，唯一即时路径是死代码 toggle-feature，技术债 M7，见上轮审查）；
   - 词库阶段台词 fd/xl/sy 各仅 4 条（低频触发，可接受）；
   - main.js 3913 行"上帝文件"（技术债 M8，拆分需谨慎，涉及 110+ IPC）。

## 六、给接手模型的提示

- **验证日语省内存生效**：`Get-CimInstance Win32_Process | Where-Object { $_.Name -match 'python' -and $_.CommandLine -match 'genie_tts_server' }` 应为空；`gsv/api.py`（GPT-SoVITS）应存在。
- **验证许可状态**：GitHub 仓库页 License 徽章可能显示 BUSL-1.1 或不识别（显示"其他"），后者仅为显示问题，LICENSE 文件内容为准。
- **xlsx 升级后续**：若 npm install 重装依赖，xlsx 会从 `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` 拉取（package.json 已是该 URL），需网络；CI 用 npm ci 走 package-lock 不受影响。
- **Busy asar 兜底**：`cp -f app.asar.new app.asar` 是可靠方案；必要时先杀 Genie/GSV 引擎残留进程（`Get-CimInstance ... genie_tts_server` / `api.py`）。
