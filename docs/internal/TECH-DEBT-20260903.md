# 技术债台账（2026-09-03 建档，同日凌晨批量清偿）

> 来源：v2.5.27 合并批次部署后的遗留项与在办调查。每项含现状、证据、下一步。
> 处理约定：一次只做一项，做完回归 `npm test`（当前基线 40/40）再部署。
> **2026-09-03 凌晨批**：TD-1/2/3/6/8/9(轻量) 已清偿（各一个 commit，40/40 全绿后统一部署一次，
> 见 HANDOFF-20260902-deploy-2.5.27.md 五点十～五点十六）；另有前置 TD-0（Mimosa 安全门禁高危清零）。

## TD-1 ✅ 已修复（62c72e5，待真机复验）：winter 皮肤坐姿"悬空"——气泡打开时画布锚点错位

- 根因（结合探针与代码走查确认）：气泡开/关走 `pet:set-size` 只改窗口高不改 y，窗口底随动；
  放大时 clamp 把 y 钳到「工作区底-高+80」，坐姿脚底随之偏离任务栏沿口；且放大暂停
  （zoomPaused）期间 walkTick 的 5s 坐姿自愈被 `!walk.paused` 挡住不跑——悬空持续到气泡关闭/起身，
  与"几秒/起身后自愈、截图时气泡展开"的现象吻合。
- 修复：判定抽到 `walkState.seatReanchorOnResizeDecision`（仅坐姿且非拖拽/飞行/跳跃/跳窗顶），
  `pet:set-size` 在 setSize 后与 150ms clamp 后各补一次 applySeatPosition——悬空可见时长 5s→0。
- 待复验：真机开/关气泡（含放大）观察坐姿是否还悬空；探针补采气泡打开态数据可作对照。

## TD-2 ✅ 已修复（2a9febf）：pendingAmbient 无过期时间

- 已加 createdAt，flush 时超 3 分钟丢弃并经 playback 记日志（`[ambient] 丢弃过期暂存搭话`）。

## TD-3 ✅ 已修复（9be8d69）：固定台词音频缓存无总容量上限

- 已加 500MB 总预算 + 指纹目录 LRU 清理（mtime 天气为最近使用信号），当前方案永不删；
  saveItem 写盘后节流触发（60s）；单测覆盖（fixed-line-cache.test.js）。

## TD-4 ⏸ 未动：ConversationService 未抽取

- 评估：跨 GUI 聊天/Agent 队列/regenerate/主动搭话/TTS 五套状态，属大重构；无人监督会话
  不宜一次吞下。建议按台账"抽单写者队列 + 统一 task ID/取消/错误码"单独立项，sendProactive
  的 lineId 透传（已上线）是现成起点。

## TD-5 ⏸ 未动：main.js 巨石与生命周期注册表

- 评估：4400 行拆域（chat/voice/settings/walk/agent/security）+ LifecycleRegistry 是多会话工程，
  本次只做了与 TD-1 相关的最小修改，未扩大战场。

## TD-6 ✅ 已修复（376906b）：自动更新器完整性校验

- 审计确认缺口属实：只比大小不校验哈希、ps1 只备份不回滚。已落地：下载后必校验 SHA-256
  （优先 GitHub digest 字段，回退 SHA256SUMS.txt；无校验来源 fail closed）；
  apply-update.ps1 重启 25s 探活失败自动回滚 .bak。单测全绿。

## TD-7 ⏸ 未动（需用户决定）：Live2D Core 属仓库外资产

- 两个方案（发布流程注入+校验 / 下架内置 Live2D 宣传并恢复 --strict）都影响发布形态，
  留给用户拍板。release.yml 仍为非严格模式。

## TD-8 ✅ 已修复（89e83cd）：固定台词面板与离线模式开关未国际化

- 约 80 个 set.* 键三语齐备（卡片静态文案 data-i18n、21 个池名+天气前缀、状态徽标、
  预加载/清理确认与结果、离线开关提示）；引擎品牌名不翻译；i18n 键一致性校验通过（320 键）。

## TD-9 🟡 轻量版已做（44c1aa4）：改名触发全量重翻译

- 设置页保存时检测称呼变化并 confirm 提示（三语），取消还原输入框。
- "模板+称呼占位符"缓存方案仍留待后续（收益大但动翻译层，宜单独立项）。

## TD-10：已知偶发项（观察即可，不修）——本批部署后日志仍见日语翻译偶发 timeout，符合预期

## TD-11：发布收尾（等用户决定）——本批 7 个 commit 在 merge-batch 分支，未推送；部署目录已含本批内容

## TD-0 ✅（2026-09-03 新增并清偿，8fd948b）：Mimosa 安全门禁高危清零

- 会话内 git commit 被 Mimosa L3 门禁拦截（12 个高危），全部为存量代码问题。逐项修复：
  translate-cache SHA-1→SHA-256；file-guard 蜜标假凭据改运行时随机 + fs.watch 路径收敛；
  storage 环境变量重定向仅接受绝对路径 + healFileAsDir 无参硬编码路径表；tts-manager
  临时文件名白名单+insideDir 守卫；cosy_tts.py / genie_tts_server.py 路径守卫与落盘 API 收敛；
  test-config-secrets.js 假密钥运行时生成。修复后 40/40 + test:secrets 36/36 全绿。
- **遗留 7 个中危**（chat-client.js ×4、zcode-client.js ×2、gif-frames.js ×1，"疑似跨文件污点"）
  需用户确认处置方式（修复或说明豁免），见交接文档。
