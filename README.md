# 🩺 苏苏洛桌宠（SuzuranPet）

> 一款以 AI 角色互动为核心的桌面陪伴应用 —— **悬浮桌宠 · 双语言真人音色 · 桌面行走 · 长期记忆 · 人设/皮肤自由**。
> 罗德岛医疗干员苏苏洛，从终端里走出来，住进你的桌面。

**免安装 · 绿色版**：解压双击 exe 即用，无需 Node / Python（不开语音时）。
聊天回复来自你自配的 OpenAI 兼容接口（DeepSeek / Kimi / Qwen / 本地 Ollama…），**程序不内置任何模型、不上传任何数据**。

---

## ✨ 核心能力

> 一句话认识她：**陪你聊天、记住你的事、用真人音色说话、在任务栏上散步，还能换上你喜欢的形象。**

| 能力 | 一句话 | 展开 |
| --- | --- | --- |
| 💬 **聊天陪伴** | 单击小狐狸就能聊 | 任意 OpenAI 兼容 API（DeepSeek/Kimi/GLM/Qwen/Ollama/Claude）；回复带情绪标注，`*动作*` 斜体渲染、Swipes 多版本切换、快捷命令（提醒/番茄钟/系统状态） |
| 🧠 **长期记忆** | 她记得你说过的事 | 规则式提取称谓/喜好/生日/健康/近期安排 + 每 20 轮自动摘要 + 羁绊系统（相处越久越亲近）；设置页可**手动添加/编辑/删除**，本地加密存储 |
| 🎭 **角色扮演开关** | 想让她撒娇还是听话，你说了算 | RP 模式（恋人/角色扮演口吻）一键关闭 → 助手模式：优先服从指令、回复直接简洁 |
| 🗣 **真人音色** | 中文日语都是苏苏洛的声音 | Genie 中文 + GPT-SoVITS 日语引擎**随包自带**，开箱即说；支持克隆任意音色、情绪音色分档、日语语音模式（界面中文、说话日语） |
| 🦊 **桌面行走** | 她会像在基建里一样散步 | 任务栏上沿为地面走走停停，偶尔一步跳上程序窗口坐一会；拖拽磁吸、桌面图标感知（可选）、相位机自愈永不卡死 |
| 🎨 **四种形象** | 表情 / 小人 / 2.5D / Live2D 随意切 | GIF 经典表情（38 款皮肤）、Spine 小人（动画语义与游戏一致）、PSD 2.5D 角色、Live2D 模型（内置示例） |
| 📅 **日程提醒** | 到点她会提醒你 | 单次/每天/每周/每月重复，支持情绪与备注，Excel 批量导入；气泡 + 情绪切换 + 语音 + Windows 通知 |
| 🔐 **安全默认** | 密钥本地加密，绝不外传 | 三槽位 DPAPI 加密存储、DLL 完整性自检、数字签名、两轮 VM 隔离安全测试；本地服务只监听 loopback |
| 🔌 **Agent 接口** | 让别的 AI / 脚本也能找她聊天 | 本地 `127.0.0.1:8765` 的 `/chat` 接口，仅监听本机，可选 Bearer Token 认证 |

## 🧩 核心机制

### 1. 记忆：从「记性」到「羁绊」的三层体系
- **事实层**：规则式提取 + 手动添加，去重、封顶、只留最近
- **摘要层**：每 20 轮对话自动生成印象摘要
- **羁绊层**：聊天/摸头 +1 经验、每日首互 +2，等级 1~10 影响亲密度与专属台词
- 全部本地 DPAPI 加密存储，设置页可视化查看/编辑/删除/清空

### 2. 语音：五级回退，绝不让话卡住
系统语音 → edge-tts → CosyVoice → 本地 Genie（中文克隆音色） → GPT-SoVITS（日语微调音色），逐级自动回退；
情绪参考音频分档（撒娇/傲娇/惊讶/温柔/开心），仅在你选择的情绪命中时才切换，默认绝不带参考音频。

### 3. 行走：任务栏就是她的罗德岛基建
以任务栏上沿为地面散步、坐下休息，一步跳到程序窗口顶上歇脚再跳回来；
拖拽暂停、抛出后会有"眩晕抗议"（😵💫）反馈；相位机自愈保险丝：定时器/mouseup 丢失自动恢复，杜绝永久静止。

### 4. 渲染：四种形态，一套引擎
GIF 经典表情（最稳）/ Spine 小人（行走）/ PSD 2.5D（贴底独立）/ Live2D（自治模块，单点可回滚）；
透明区域完全点击穿透，不挡任何软件；置顶层级周期重断言，全屏游戏也能自己回到最上层。

### 5. 安全：默认安全，不是口号
密钥三槽位 DPAPI 加密；本地 TTS / Agent 服务只监听 loopback；DLL 基线自检 + 数字签名；蜜标监控检测外部程序访问敏感配置区域。

## 🚀 快速开始

> 🐣 **完全新手？** 图文手把手教程：[`docs/快速开始.md`](docs/快速开始.md)（Key 去哪领、怎么填、常见故障怎么办）

1. 解压整个文件夹（建议放桌面或喜欢的位置）
2. 双击 `苏苏洛桌宠.exe`，首次启动弹《使用条款》→ 同意
3. 自动弹出设置：**① 聊天 API** 选服务商 → 填 API Key →「测试连接」→「保存」；把「桌宠对你的称呼」改成你的名字
4. 单击小狐狸 → 输入框 → 回车，开始聊天

## 🔌 支持的服务商

| 服务商 | baseUrl | 说明 |
| --- | --- | --- |
| DeepSeek | `https://api.deepseek.com/v1` | 便宜好用，推荐 |
| Kimi（月之暗面） | `https://api.moonshot.cn/v1` | 长上下文 |
| OpenAI | `https://api.openai.com/v1` | 需科学上网 |
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | 国产免费额度 |
| 阿里云百炼 Qwen | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 国产多模型体验 |
| 硅基流动 | `https://api.siliconflow.cn/v1` | 聚合多家模型 |
| Ollama（本地） | `http://localhost:11434/v1` | 免费、不联网、无需 Key |
| Anthropic Claude | `https://api.anthropic.com` | 协议类型选 Anthropic |

> 只要是 OpenAI 兼容接口，都能在「自定义」里填 baseUrl + 模型名直接用。

## 🗣 语音（可选，默认关闭）

想要她开口说话？托盘右键 →「🎤 语音部署与训练」打开指南，四种方案任选：

| 方案 | 需要什么 | 效果 |
| --- | --- | --- |
| 系统自带语音 | 零配置 | 最省事 |
| edge-tts 云端 | 装 Python + `pip install edge-tts` | 微软语音 |
| **本地克隆音色（推荐）** | 引擎随包自带 | 苏苏洛真人音色，中文（Genie）+ 日语（GPT-SoVITS）开箱即用 |
| 百炼 CosyVoice | 填 API Key + 音色 ID | 云端克隆 |

**日语语音模式**：设置 → 语音 → 勾选「🗣 日语语音模式」——气泡文字仍是中文，回复先翻译成日语，再用日语音色合成播放（称呼如"博士/主人"会被强制音译成 ドクター/マスター，不会读漏）；日语服务不可用自动回退中文。

## 🎨 换形象 & 换人设

- **四种渲染模式**：设置 → 渲染模式（GIF / Spine 小人 / 2.5D / Live2D），随意切换
- **换皮肤**：托盘「🧩 人物」菜单一键切换 38 款皮肤；把自己下载的 Spine 模型（`.atlas` + `.skel/.json` + `.png`）放进皮肤文件夹，重启即出现，无需配置
- **换人设**：托盘 → 设置 →「② 人设」直接编辑，`{{userName}}` 自动替换成你的称呼，保存立即生效
- **自定义情绪**：表情管理里自己加情绪（≤5 个字，最多 30 个）并配 GIF，AI 回复时自动选最贴切的表现

## 📂 目录说明

| 路径 | 说明 |
| --- | --- |
| `苏苏洛桌宠 2.5 正式版.exe` | 主程序（双击运行） |
| `engines/` | 语音引擎（Genie 中文 + GPT-SoVITS 日语，约 15GB；**删了就不能说话**，聊天不受影响） |
| `使用说明.html` | 入门文档（就是这份的网页版） |
| `新手教程/` | 图文版手把手教程 |
| `语音部署与训练指南/` | 语音方案部署 + 音色克隆训练教程 |
| 用户数据 | `%APPDATA%\苏苏洛桌宠 1.1 正式版\`（聊天记录/记忆/设置/密钥，不随安装目录） |

## ❓ 常见问题

- **双击没反应 / SmartScreen 拦截**：点「更多信息」→「仍要运行」（程序未购买微软信任章，非病毒）
- **提示没配 API Key**：托盘 → 设置 → 填 Key → 测试连接
- **聊天没反应**：先单击小狐狸打开输入框；或 API 欠费 / 模型名不对
- **想开任务模式（/zcode 前缀操控电脑）**：需另装 ZCode，然后在 `config.json` 里把 `zcodeEnabled` 改为 `true` 并填 `zcodeCli` 路径（默认关闭）
- **电脑时间不准**：联网请求会失败，请开启系统"自动设置时间"

## ⚠ 重要说明

- **本程序不内置任何 AI 模型**：聊天回复来自你自配的 OpenAI 兼容接口（第三方服务）。API Key 只存在你本机（DPAPI 加密），不上传、不读取你的其他密钥。
- **首次运行被 SmartScreen 拦截**是未购买商业代码签名证书的正常现象，点「仍要运行」即可。
- **引擎随包约 15GB**：完整版含引擎，轻量版不含（可自行按《语音部署与训练指南》部署）。
- 隐私政策见 [`PRIVACY.md`](PRIVACY.md)，使用条款见 [`TERMS.md`](TERMS.md)，许可证见 [`LICENSE`](LICENSE)。

## 📜 更新日志

完整更新历史见 [`CHANGELOG.md`](CHANGELOG.md)。

---

感谢 [High-Logic/Genie](https://github.com/High-Logic/Genie/tree/master) 的开源。
