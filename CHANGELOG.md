# 更新日志

## v2.5.26（2026-09-01）

### 🎨 UI 优化：暗色主题闭环 + 主窗口质感 + 辅助窗口统一

- **主窗口令牌化**：pet.css 50+ 处硬编码颜色全部收进 `--pet-*` 双主题变量，深浅自动翻转；缩放/关闭按钮恢复半透明幽灵样式（原被级联覆盖成实心圆属意外），停止按钮恢复红色语义；阴影升级为环境影+近影双层
- **辅助窗口暗色闭环**：新增 `theme-init.js`——说明/快速开始/条款/日程/PSD 工具/添加人物/表情管理/音色克隆 8 个窗口此前拿不到 `theme-dark`（暗色只在主窗口、设置、文档中心生效），现已全部跟随主题；使用说明窗口补齐缺失的 preload
- **文档中心风格统一**：浅色由橙色系统一到全局青系；深色下 iframe 不再白底；选中导航项对比度修正
- **PSD 工具窗口**：整页样式令牌化，新增深色主题（透明预览棋盘格双主题）
- **细节与可访问性**：iOS 开关深色轨道与键盘焦点环、设置导航焦点环、Swipes 切换按钮过渡/按压反馈、`color-scheme` 原生控件跟随主题、深色下警告/危险色对比度修正、条款页眉深色对比度
- 纯视觉改动：拖拽/穿透/行走/气泡定位等交互逻辑零触碰；22 个单测全绿
- **质感二批**：气泡尾巴边框融合（双层三角）、退出 120ms 淡出（旧内核自动回退）、毛玻璃渐进增强、按压/眩晕动画尊重减少动效、小字对比度微调
- **主题逻辑收敛**：auto=19-6 点规则收进 `renderer/theme.js` 唯一来源（原四份拷贝）；主题切换改为全窗广播，辅助窗口实时跟随；新增 9 断言单测
- **可访问性**：Excel 导入预览弹窗焦点圈定 + Esc 关闭 + 焦点回还
- **清理**：moods「自定义」标签与 PSD 棋盘格的内联样式改类名/CSS 变量
- **设置页打磨**：标题颜色统一（去首字变色）、间距 4px 栅格规范化、分区 stagger 入场
- **固定台词优化**：默认称呼统一「博士」（与硬编码台词/IP 设定一致，设置页可自定义）；9 处近重复台词去重；含糊台词改写；省略号规范
- **应用内文档中心**：4 个 iframe 文档统一青系 + 深色适配，主题实时同步进 iframe
- **官网品牌统一**（gh-pages）：换与应用同款青系深色
- **工程**：引入 stylelint（`npm run lint:css`），修 3 处重复选择器
- **设置页重排**：「系统与高级（杂七杂八）」拆为 系统界面 / Agent 接口 / 感知监控 三区；模式专属控件归位渲染模式区；记忆/情绪开关归位各自分区；导航与三语 i18n 同步
- **设置页对齐修复**：开关行改横排（文字左、开关右，修复「开关居中掉到文字下方」的级联陷阱）；复选框文字与框同行；滑杆数值贴滑杆右侧
- **设置页二轮优化**（用户反馈）：开关置顶对齐首行；记忆管理改全宽；情绪音色试听改子标题+横排徽章组
- **固定台词扩充**：句库 248→293 条（摸头/抛掷/睡醒/各时段/散步/坐着/闲置/羁绊阶段全池补新，保持人设口吻）
- **台词情绪→音色分档**：新增 `LINE_MOODS` 池级情绪映射——主动搭话按由头（清晨/羁绊/闲置/时段/状态）、人格化按事件（被抛=惊讶、被逮=傲娇、睡醒=温柔）、工作流=温柔、日程按情绪映射，播报全链路把情绪传给 GSV 选对应音色（此前大量传 idle/love 等无效值，永远默认音色）
- **台词念白合理性**：固定台词的（动作/舞台指示）只显示不朗读（中/日两条 TTS 链路+日语预热全接入）；翻译提示词按日常口吻处理拟声词
- **台词级情绪细标**：行首【情绪】标记覆盖池默认（如温柔池里的俏皮句标傲娇），标记不进气泡与朗读
- **摸头情绪递进**：6 秒内连摸 3 次起音色切撒娇档（越摸越黏），停手重置
- **日语预热进度可视化**：设置页语音区显示「已缓存 x/N 句」实时进度
- **设置页滑杆主题化**：自绘轨道/圆钮（变量驱动，深色下不再刺眼），悬停放大+键盘焦点环
- **自查修复**：2 条纯动作台词补正文（原剥动作后为空被静音）；主进程 TTS 空串防御；预热跳过空句
- **官网**：新增深浅色切换按钮（变量驱动 + localStorage 记忆偏好）
- **主动搭话重复感修复**：池内去重 K 随池大小自适应（小池 3、大池至 1/3）；新增跨轮禁选（最近说过的 8 句不再抽，banned 绝对优先、仅池被全覆盖才破例）；记忆事实由头（健康/称谓/日程/里程碑/生日）从固定单句改 2-3 变体随机；预热补上散步/坐着池
- **i18n 补齐**：新增设置文案（试听标题/音色启用/预热进度）中英日三语
- **README_EN 全量同步**：提供商表、技术细节、语音对比、数据位置、FAQ、文档链
- **视觉回归测试**：Playwright + chromium，设置页结构断言+深浅截图存档（`npm run test:visual`）
- **官网 guide 文档**：加深浅切换按钮
- **感知与监控区布局再修**：复选框顶对齐首行+半宽；开关行半宽（长文案不再挤成多行）
- **坐姿自愈**：坐下后 Y 漂移 5s 低频拉回任务栏（带诊断日志）；自主坐下加日志
- **托盘「一键坐到任务栏」**：原死 API 接进动作子菜单，手动归位/验证坐姿

## v2.5.25（2026-09-01）

### 🔒 安全加固（第三方优化建议 P0/P2）

- **SSRF 防护**：聊天 API 的 baseUrl 请求前校验目标主机——放行回环（本地 Ollama/聚合站），拒绝内网/链路本地/ULA 地址（防配置被篡改/钓鱼后请求内网）。确需内网网关的用户可在 `config.chat.allowPrivateBaseUrl=true` 显式逃生
- **Agent API 写操作强制认证**：`/chat`、`/stop`（消耗 LLM 额度/改状态）始终要求 Bearer token——即使自动生成 token 失败（安全存储不可用）也不开放无认证写入；`/health`、`/status` 只读保持开放
- **Agent API Content-Type 校验**：显式非 `application/json` 的请求直接 415（缺失时放行，兼容 curl -d 老脚本）
- **蜜标告警限频 30s→10s**：勒索/篡改类 30s 静默期太长，攻击者可在静默期内完成大量加密；10s 仍能压住 fs.watch 重复事件噪音

### 🐛 行为修复

- **config 篡改恢复持久化**：干净配置基线从内存改为落盘 `config.clean.json`——进程被杀后重启，若磁盘 config 已被外部篡改，会用上次干净副本恢复而非把篡改内容当新基线
- **SSE error 帧**：OpenAI 兼容流中 `data: {"error": ...}` 帧不再静默忽略，向上抛出并中断流（此前错误会被吞掉，表现为回复戛然而止且无提示）

## v2.5.24（2026-09-01）

### 🐛 行为修复

- **关语音停 GSV 引擎（回归）**：此前语音关只停 Genie 中文引擎、GSV 日语引擎残留占显存；且从设置页保存关语音只广播不停任何引擎。已抽 `applyTtsEngine()` 供托盘/按钮/设置页三入口共用——关语音时 Genie + GSV 一起停（GSV 按 api.py 进程特征精准杀）
- **换肤后"拿不起来"（回归）**：切换皮肤时销毁旧 PIXI 画布（WebGL context）触发 `webglcontextlost`（低配核显实测，销毁阶段即触发）→ 误走整页 reload 自愈 → 交互被打断。换肤全程置 `skinSwitching` 标志吞掉旧 context 销毁的 lost（新画布随后重建，不 reload）；另加销毁后 200ms 延迟再重建 + reload 后 init 完成立即 `setClickable(true)` 恢复可交互

### 🎨 渲染

- **换肤重建缓冲**：旧 context 释放与新 context 创建不再同帧交替（200ms 间隔），降低低配核显 lost 概率

## v2.5.23（2026-09-01）

### 🚨 紧急修复（上版引入的回归）

- **保存 API Key 失效回归（复审新-1）**：v2.5.22 白名单误杀 `secrets` 键——设置页填的 Key 被静默丢弃却提示"保存成功"。已放行 `secrets`（走 DPAPI 通道不入 config.json，提取只认三个已知槽位的 replace action，无安全影响），并把白名单过滤抽成 `src/settings-patch.js` 纯函数 + 单测覆盖"保存 Key → replaceSecrets 收到值"

### 🔒 安全加固

- **P1-7 killPortListener 身份校验**：强杀 GSV 残留进程前校验进程名（必须 python 系）+ 命令行含 serverScript 特征，杜绝误杀 9880/9881 同端口被其他程序占用
- **P1-3 收尾 rig-apply 校验**：同 apply-gif，只允许 .psd + realpath 拒绝符号链接/越权路径（堵任意文件读取链）
- **新-2 Live2D 缺失提示修复**：index.html 内联 onerror 被 CSP script-src 拦截永不执行，降级提示移到 pet.js initLive2d 主逻辑检测（缺失时 toast 提示 + 降级 GIF）

### 🐛 行为修复

- **新-3 Spine 启动竞态**：渲染库 defer 后启动即 Spine 模式时 PIXI 可能未就绪——initSpine 顶部等待就绪（最多 8s），超时才降级 GIF（此前静默回退）
- **P1-8 日语翻译 baseUrl 规范化**：复用 chat-client 的 normalize——填 `api.deepseek.com`（无 /v1）自动补全不再 404；Anthropic 填带 /v1 的不会拼出 `/v1/v1/messages`

### ⚡ 性能

- **P1-5 历史记录轮转**：`history.jsonl` 从"只追加不收缩 + 每次读取全量解析"改为内存滚动窗口（启动载入一次常驻）+ 上限 4000 条自动裁掉最旧；长聊后聊天延迟不再持续恶化，"每 20 轮记忆摘要"的 999 条全量计数改走内存 `count()`

## v2.5.22（2026-08-31）

### 🔒 安全加固（重要，建议尽快升级）

- **P0-1 渲染层配置写入白名单**：`pet:save-settings` 仅允许设置页可改的键；`zcodeCli/workspace/zcodeEnabled/translateApi` 等会被当可执行路径 spawn 的字段禁止从渲染层写入——堵住"渲染层 XSS 冒用 saveSettings 植入可执行路径"的任意代码执行链
- **P0-2 硬件加速条件化**：删除无条件 `disableHardwareAcceleration`，设置页「软件渲染」开关真正生效
- **P1-3 apply-gif 校验**：只允许 .gif 扩展名 + realpath 拒绝符号链接（堵任意文件读取链）
- **P1-4 Agent 零认证自动令牌**：首次启用 Agent 接口自动生成随机 Bearer token（DPAPI 加密），杜绝本机进程无认证消耗 LLM 额度
- **P1-6 maxHistoryTurns 语义修正**：轮数/条数混淆导致上下文砍半，已修正

### ⚖️ 许可修正

- **LICENSE 换官方 BUSL-1.1 模板原文**：Terms + Covenants of Licensor 逐字对齐 SPDX 官方文本（此前为自写改写版，缺失显著展示/违反终止/商标/免责大写等官方条款）；仅替换参数值

### 🎨 渲染与性能

- **渲染库 defer 加载**：GIF 模式启动不阻塞（PIXI/Spine/Live2D 约 1MB 后台加载，切模式时已就绪）
- **工作区监听改 fs.watch**：事件驱动替代每 5s 全树同步遍历（大目录不再周期性卡死主进程）
- **打字机增量渲染**：长回复从 O(n²) 全量重建改为 O(n) 增量 textContent，完成后再富渲染一次
- **Agent lastSeen 60s 节流落盘**：不再每次请求全量重写 config.json（磨损 SSD）
- **vector-memory 阈值收紧**：余弦 0.06 → 0.10，滤掉弱相关片段

### 🐛 行为修复

- **睡觉触发修复**：睡眠计时提前到初始化，未配 API Key 也会困（此前被 return 跳过永远不睡）
- **睡觉悬浮修复**：躺下不再上抬窗口（此前窗口上浮导致角色悬空），恢复站立贴地位置
- **小人脚在任务栏前后不稳定**：置顶重断言补 `screen-saver` 层级（无 level 会每 5s 降级致任务栏盖脚）+ 行走移动时按几何重判置顶

### 📦 供应链

- **xlsx vendor 本地化**：从 CDN tarball（无完整性校验）改为仓库内 `vendor/xlsx.js` + `vendor/dist/cpexcel.js`（Apache-2.0，附许可证，见 P1-3 说明）
- **硬编码路径改 os.homedir()**：5 处 `C:\Users\xsbil` 兜底替换
- **删除死代码**：`detectApiKeyFromZcode` / `detectDashScopeKey`（无调用，且与 README「不读取其他密钥」承诺存在误解空间）

---

## v2.5.20（2026-08-31）

### 🎙 日语翻译预热

- **固定台词预翻译**：165 句不含人称的台词启动后空闲批次预翻译进磁盘缓存（每批 3 句 15 秒间隔，约 15 分钟跑完）——翻译 API 挂了也能说出日语（"说不出来"根治）
- **磁盘缓存优先级修复**：`translateToJa` 磁盘缓存提到 apiKey 检查之前——key 失效时已翻译台词仍可用

---

## v2.5.19（2026-08-31）

### 🎙 语音稳定性

- **重复台词翻译失败不再固定走系统音**：翻译失败进「失败池」10s 冷却，冷却后同句再次触发自动重新翻译（此前失败结果缓存 90s，期间重复台词一直走系统音）
- 成功译文仍 10 分钟缓存复用，不增加 API 调用

### 🚀 发布链路（GitHub）

- **release.yml 动态版本号**：zip 名/Release 标题随 tag 自动生成（不再发成「1.1 正式版」）
- **Release 正文自动生成**：从 CHANGELOG 抽取本版本段落 + 统一下载说明（体积/皮肤包/完整版邮箱），不再被旧模板覆盖
- **v2.5.18 已发布**（首个 CI 自动构建包，冒烟验证通过：全新环境 config/persona 正常生成、同意条款后进程存活）

### 📄 许可与合规

- **LICENSE 换官方 BUSL-1.1 模板原文**：补全 9 个官方条款（显著展示/违反自动终止/商标/免责大写格式/Covenants of Licensor 等），只填参数值

### 🏠 仓库门面

- 12 个 topics + homepage 指向官网（GitHub Pages 已上线）
- GitHub Pages 官网（开箱必读/使用说明/API 接入/语音部署 4 文档 + 入口页）
- `deploy/sync-pages.sh`：改完文档一键同步官网
- FUNDING.yml + 4 个 Issue 模板（Bug/功能建议/皮肤投稿/联系）
- xlsx 漏洞 Issue 已回复并关闭

### 🎛 体验

- 设置窗 720 → 900 宽（侧栏导航不再挤压内容区）
- 教程统一到 `新手教程/`（更新至 2.5 正式版），删除双拷贝

---

## v2.5.18（2026-08-31）

### 🐛 首启「同意后退出」修复（本轮核心）

- 根因：点「同意并开始使用」→ 正常关窗被 `beforeunload` 无条件当成「拒绝」→ `app.quit()` 退出，未进入引导设置窗
- `renderer/terms.js` 加同意标志位：点过同意后的关窗不再触发拒绝退出
- `openSettings()` 补 `show()/focus()`：引导设置窗不再停在隐藏位（-32000）用户看不到
- `storage.js` copyIfMissing 单文件修复：全新安装 config.json/persona.md 不再被建成空目录（持久化失效 P0）+ 空目录自愈

### 🚀 产品名升级与数据迁移

- `productName`：「苏苏洛桌宠 1.1 正式版」→「苏苏洛桌宠 2.5 正式版」，版本号 2.5.0 → 2.5.18
- 内置 userData 迁移：旧 productName 目录存在时自动 rename 到新目录（或回退旧目录），**老用户数据不丢失**

### 🎛 托盘菜单分组重构

- 原 23 项平铺 + 3 层嵌套 + 禁用项当标题，收敛为「高频开关区 → 常用窗口区 → 外观/动作/语音三个子菜单 → 高级子菜单 → 退出」
- 语速/大小改为子菜单内 radio，去掉"像坏了的功能"的观感；设置页与 i18n 配套更新

### 📖 文档

- 新增英文 README（README_EN.md）；过程文档归档至 `docs/internal/`；开箱必读/教程更新

---

## v2.5.0（2026-08-29）

从 2.2 到 2.5 的大版本合集（完整变更见 docs/internal/optimization-progress.md §49-§111），核心亮点：

### 🎙 语音引擎完整随包（开箱即用）

- 中文（GenieTTS 克隆音色）与日语（GPT-SoVITS v2Pro）推理引擎与苏苏洛训练成品全部随包（engines/ 约 15GB），完整版新用户解压即说话，无需安装任何环境
- 首启自动探测引擎路径并回填配置；Genie venv 自动修复（包内 base-python）；VC++ 运行库随包（新机器 onnxruntime 可用）
- 完整版出厂默认开启语音，轻量版（GitHub Release，不含引擎）默认关闭、部署引擎后可开；配置了「用完即停」式安全默认（无 API Key、无宿主路径、agreed=false 首启弹条款）

### 📖 内置文档中心

- 托盘右键即可阅读新手教程（9 篇）、使用说明、语音部署指南、API 接入指南（学生白嫖版：阿里云百炼学生券、智谱免费档、硅基流动、Gemini、OpenRouter 等）
- 轻量 md 渲染器，离线无依赖

### 🎭 Live2D 渲染模式（第四渲染态，实验）

- 支持 Cubism 3/4/5 模型（pixi-live2d-display），内置 haru 示例模型
- 贴底等比显示、自动 Idle、点击/放下有动作反应、聊天情绪联动动作、拖拽正常
- 设置页可选择模型（userData/assets/live2d/ 放入即用）；Cubism Core 随包捆绑

### 🎬 2.5D（PSD rig）与皮肤

- 2.5D 渲染回退稳定版；维什戴尔三套等 38 个皮肤全量内置
- 皮肤支持删除管理（§96）

### 🔒 安全

- DLL 完整性自检 + 数字签名 + 启动签名校验（S05 根治）；VM 隔离环境两轮安全测试（网络攻击 9 项、注入、MITM 等）
- 发布包干净化：无宿主路径残留、无开发回退件、出厂配置纯净

### 🧹 其他

- 渲染管线回退与稳定性修复；行走几何收敛（src/walk-geo.js + 单测）；防御模块单测
- 发布流程 7-Zip 打包 + 出厂 SOP（deploy/README）

## v2.2.1（2026-08-25）

安全加固与日程导入体验改进：

### 🛡️ 蜜标监控（新功能，默认关）

- 检测其他程序访问桌宠的敏感配置区域：在用户数据目录部署蜜标文件，被读取时桌宠立即气泡提醒"检测到有程序访问了我的敏感配置区域"
- 托盘「🛡️ 蜜标监控」/ 设置页开关控制（默认关闭）
- 说明：普通权限下无法显示具体进程名（ETW 需管理员）；蜜标文件桌宠自身不读取，不会自误报

### 🐈 逗猫棒（新功能，需许可）

- **角色追着鼠标走**：开启后桌宠在地面朝鼠标位置水平移动（走路动画），到达鼠标正下方后站立，鼠标移动就继续追——像猫追逗猫棒
- 因需读取鼠标位置，与「全局鼠标跟踪」同属**需显式许可**功能：默认关闭，设置页顶部「🧪 实验性功能」区块有独立开关（带"仅本机·不上传"授权说明，Spine 模式显示），托盘「🐈 逗猫棒」开关同样可用
- 开启时自动启用行走引擎，关闭后恢复普通行走节奏

### 🗣 日语稳定性

- **翻译缓存**：相同文本 90 秒内不重复调用翻译 API（LRU，容量 200）——减少上游限流(429)压力、重复句响应更快；翻译失败也缓存 90 秒，限流窗口内不反复撞、恢复后自动重试

### 🖼 设置与托盘整理

- **设置页按渲染模式动态显示**：「渲染模式」选择器移到设置页顶部第一区块，选中 GIF / Spine / 2.5D 后仅显示该模式支持的选项（Spine→行走/模型，2.5D→角色/跟随/全局跟踪，GIF→经典表情），界面不再杂乱
- **托盘菜单按渲染模式同步**：Spine 模式显示人物皮肤/桌面行走/动作试演/散步速度，2.5D 模式显示 2.5D 开关/皮肤/PSD 工具，GIF 模式两者都不显示；切换模式后托盘菜单自动刷新
- 修复：托盘菜单重构时 2.5D 开关/皮肤菜单项因 spread 单对象导致 `refreshTrayMenu` 抛 TypeError、托盘菜单整体不弹出的回归

### 🗣 日语稳定性

- **翻译 429 限流修复**：限流等待从固定上限 15s 改为按服务端 `retryAfterSeconds` 等待（上限 90s），覆盖常见 60s 限流窗口——此前 15s 后重试必再 429，导致日语退回中文

### 🎬 2.5D 实验性更新

- **头部与眼睛跟随鼠标**：2.5D 角色会转头、转动眼睛看向鼠标位置（类似 Live2D 追踪），鼠标离开后自然回到前方
- **全局鼠标跟踪（需许可）**：鼠标在屏幕任意位置，角色都转头/转眼看向它（不限于桌宠窗口内）。因需读取屏幕鼠标位置，作为隐私敏感能力**默认关闭**，须在设置页显式开启「🖱️ 全局鼠标跟踪（实验性）」；仅本机计算、不上传任何数据
- **跟随能力自适应算法**：换 PSD 后自动评估该皮肤支持哪种跟随（眼白+虹膜左右分离完整→头+眼跟随；只有脸部结构→仅头部跟随；异常→关闭），眼睛结构缺失时自动降级、不会失效；日志输出当前皮肤的支持级别，便于排查
- 设置页「🎬 2.5D 头部/眼睛跟随鼠标（实验性）」开关，默认开启，可随时关闭；实时生效无需重启
- **实验备份**：改动前快照在 `backups/2026-08-25-mouse-follow-v1/`（含回退说明），可随时回退
- 该能力由 rig 运行时的鼠标跟随参数（angleX/angleY + eyeX/eyeY）驱动，此前已内置但未开启

### 🔐 安全加固

- 全部 9 个界面页面统一补强 CSP：新增 `base-uri 'none'`、`object-src 'none'`、`frame-ancestors 'none'`、`form-action 'none'` 四个限制指令
- 补上 PSD 角色工具窗口（psd.html）此前缺失的完整 CSP，与其余页面一致
- 经 CDP 实测验证：PSD 窗口脚本（含 ag-psd 等 5 个脚本）与主窗口在加固后全部正常加载，页面渲染无异常

### 📅 日程

- Excel 导入新增**导入前预览**：选好文件后先弹窗展示文件名、总条数与前 20 行内容（标题/日期/时间/重复/情绪/备注），确认无误再写入，避免误导入
- 预览与导入拆分为两段式 IPC，解析复用同一套校验逻辑（≤500 条、单工作表、≤5MB）；预览不落库，确认后才写入
- 预览表格用 textContent 渲染，含特殊字符的行（如引号、尖括号）不会注入 HTML

## v2.2.0（2026-08-25）

全新的 PSD 2.5D 角色模式 + 聊天气泡布局修复：

### 🎬 PSD 2.5D 角色（新渲染模式）

- **完全独立的第三种渲染模式**：与 GIF / Spine 互斥、互不干扰，在设置页「渲染模式」下拉选择，或经托盘「🎬 2.5D 角色」开关启用
- **真 2.5D 网格变形**：基于 Anime2.5DRig 的 WebGL 运行时（mesh warp），支持发丝物理、眨眼等活体细节；PSD 图层自动装配为可动角色
- **皮肤管理**：设置页已导入皮肤列表单选切换；托盘「🎬 2.5D 皮肤」快捷列表；PSD 工具窗口支持导入 → 2.5D 预览 → 一键应用到桌宠
- **大小滑杆**：0.3~1.5 实时生效，窗口尺寸随角色大小联动
- **独立聊天框布局**：角色贴底大画布居中显示、可拖动；气泡在左上角（角色贴屏幕左缘时自动翻到右上角）；输入框固定底部；⤢ 放大气泡时角色自动让位
- 用户自定义 PSD 经 `pet-user://` 协议加载，存于用户数据目录，不污染程序资源

### 🛠 聊天气泡布局修复（所有渲染模式受益）

- **修复气泡内大片空白**：根因是对话框容器开启了 `white-space: pre-wrap`，HTML 源码中按钮间的缩进换行被保留成约 70px 的幻影空行，把正文推出气泡可视区。现改为仅对正文文本保留换行
- **修复 rig 模式气泡被压成细条**：非放大态高度上限原值小于最小高度被击穿，长消息只能滚动查看一条缝；现已给足可读空间且不遮挡角色主体
- **修复 rig 模式误用「气泡宽度」设置**：设置页的固定气泡宽度是给 GIF/Spine 大窗口用的，此前会溢出 2.5D 小窗口；现 2.5D 模式下由布局自动控制
- 切换到 2.5D / 关闭回原模式时，气泡尺寸状态正确切换，不再残留旧的内联样式

### 🧹 其他

- 清理临时诊断日志（画布采样每 3 秒一条、滑杆输入逐条记录等），日志回归安静
- 版本号升至 2.2.0

## v2.1.0（2026-08-25）

体验与趣味升级 + 新的 PSD 角色工具窗口：

### 🧩 PSD 角色工具（新窗口，v2.1 更新点）

- 新增独立窗口「🧩 PSD 角色工具」（托盘菜单进入）：拖入/选择分层 PSD 文件
- 解析图层树（名称/尺寸/分组/可见性），支持图层组递归展示
- **扁平化合成预览**：按图层顺序/透明度把可见图层合成整张立绘
- **导出 PNG**：保存到用户数据目录 `assets/psd-export/`，可作静态立绘/皮肤素材

### 💗 陪伴与互动

- **陪伴时间统计**：自动记录首次启动时间，信息版展示「已陪伴 X 天 X 小时」
- **信息版**：输入栏新增 📋 按钮，弹出面板聚合展示陪伴时间 + 今日日程
- **鼠标逗宠互动**：鼠标在角色附近停留约 1 秒，她会播放互动动画回应你（冷却 8 秒）
- **日程提醒提示音**：日程到点播报双音提示，提醒更有存在感

### 🛠 其他

- 版本号升至 2.1.0

## v2.0.1（2026-08-25）

2.0 正式版发布后的稳定性修复与体验改进：

### 🐛 关键 Bug 修复

- **Spine 拖动时小概率左右拉伸**：根因有两处——① `geometryReportTimer` 未声明导致渲染层启动即抛错、Spine 模式从未初始化；② 角色朝向翻转 `spineFaceDir` 用未缩放的基准缩放覆盖 `scale.x` 却保留 fit 收缩后的 `scale.y`，破坏等比造成横向拉伸。已修复并保留等比自愈兜底。
- **左侧「空气墙」**：三个叠加问题——① `Math.round` 把 `-0.4x` 四舍五入成 `-0`，Electron `setPosition(-0)` 触发 conversion failure 导致窗口移动失败卡死；② `charInset` 上报被硬性截断 119px，左边界补偿不足；③ 早期「x 最小强制 1」抵消了负坐标贴边。全部修复，角色可贴到屏幕左缘。
- **桌宠「消失」/出屏幕**：被拖出屏幕后无机制拉回。新增水平/垂直出屏兜底（角色条带左缘 <0 或窗口底超出工作区自动钳回），且屏幕外位置不再写入记忆。
- **边坐着边移动（坐着滑行）**：聊天（busy）时渲染层不切走路动画，而主进程不知道在对话、照常推进散步相位，导致 Sit 姿势被窗口带着走。修复：对话期间暂停散步（`chatPaused` 独立于拖拽暂停），结束统一恢复。
- **语音框提前消失**：`scheduleBubbleHide` 语音播放中重查只检查 `busy` 不再检查 `isSpeakingAudio`，长语音时气泡被提前关闭。改为语音/思考期间持续等待、播完才隐藏。
- **渲染进程崩溃静默消失**：新增 `render-process-gone` 自动重载恢复 + 崩溃详情上报（exitCode/原因 + crashReporter minidump），连续 3 次停止自愈并提示。

### 🗣️ 语音

- **刀客塔发音**：日语模式强制术语由英文 `doctor` 改为片假名 `ドクター`（do-ku-tā，游戏「刀客塔」发音）；中文朗读「博士→刀客塔」（仅朗读，气泡仍显示原文）。
- **日语 TTS 存活率**：单句请求超时 120s→60s（引擎挂死快速失败）；翻译 429 限流按 `retryAfterSeconds` 等待重试；单句失败不再整段回退中文——跳过失败句、其余照常合成拼接，全部失败才回退。
- **情绪语气词**：朗读文本按情绪注入语气词（开心/生气/委屈/思考等），让 TTS 带着语气读；受设置页「情绪语音」开关控制（语速/音调/语气词统一开关）。

### 🖱️ 交互

- **角色贴屏幕左缘时气泡/输入栏自动翻到右侧**（条带切边 + 窗口平移补偿，角色位置不瞬移；含输入栏同步、崩溃重载后布局恢复）。
- **任务栏吸附权重调低**：拖动中完全不磁吸（含置顶模式），松手才判定；坐姿磁吸带 48→24→12px，半挂范围收窄。
- **开机自启**：设置页新增开关，随系统启动（配合「启动隐藏」静默进托盘）。
- **位置记忆增强**：屏幕外/异常位置不保存，避免重启恢复到屏幕外。
- 诊断日志清理：移除限频 diag 日志，保留错误上报监听与等比自愈兜底。

## v2.0.0（2026-08-24）

苏苏洛桌宠 2.0 正式版，相比 1.1 的主要改进：

### 🎙️ 语音（重点大改）

- **日语语音质量全面治理**：修复句间停顿静音未按 16bit 帧对齐导致的整段样本错位电音、拼接边界咔哒爆音、8kHz 窄带啸叫、播放端三处杂音来源
- **日语语调自然化**：保留句尾衰减、真人节奏停顿、按人声电平回溯裁剪句间杂音
- **合成稳定性**：合成串行队列 + 重试退避 + 崩溃当场自愈重试本句 + 播放互斥；GSV 弃用半精度（fp16 偶发数值不稳定），改全精度
- **GSV 引擎可靠性**：设备自动检测（有 N 卡用 CUDA、否则 CPU）、启动预热引擎、单飞防重入、连续失败熔断回退中文、日语引擎死亡后自动清理挂死进程并重置
- **Genie 引擎防劣化**：参考音频切换去重（LAST_REF_AUDIO）、mkstemp 安全修复
- **TTS 端点安全**：本地 TTS 服务器只允许 loopback，远端需显式 allowRemote + HTTPS
- **密钥安全**：CosyVoice API Key 迁入 Windows 系统级加密存储（DPAPI），不再明文进配置

### 🦊 桌面行走与交互

- 坐姿腿垂进任务栏（按尺寸档位可调下沉量 + 设置页滑杆微调）
- 任务栏行走节奏改为「走路 → 坐下休息」；拖拽磁吸坐下（任务栏 / 桌面图标网格）
- 桌面图标感知（可选授权，默认关）：走到图标上站/坐，图标吸附松手判定
- 相位机自愈保险丝：定时器丢失、mouseup 丢失自动恢复，杜绝永久静止
- 聊天气泡主动关闭按钮、大小好调、字体自定义（支持导入本地字体）
- 显示层级分级（置顶 / 桌面）、一键坐到任务栏

### 🎨 角色与皮肤

- 托盘「人物」三级菜单（角色 → 形态 → 皮肤），新增 Mon3tr、迷迭香、夏季/冬季时装等
- 皮肤缩放修正体系（Mon3tr、迷迭香等包围盒偏差修正），初始缩放测量有效性防护
- 未知模型动画自动分类，`defaultMix=0.25` 降低动画硬切

### 📅 日程提醒（新增）

- 设置单次 / 每天 / 每周 / 每月重复提醒，支持情绪、备注
- Excel 批量导入（新增 xlsx 依赖）
- 到点气泡 + 情绪切换 + 语音合成 + Windows 通知

### 📖 新手体验

- 内置「新手教程」页（托盘入口 + 使用说明横幅），覆盖 API Key 领取/填入/一键导入/日常玩法/故障诊所/密钥安全
- 新增「桌宠人设模板」，方便自定义角色

### 🔐 安全与数据

- 密钥安全存储（chat / Cosy / Agent 三槽位，DPAPI 加密），设置页「⑤ 密钥与凭据安全」统一管理
- 显式凭据导入：从 ZCode/DashScope 一键导入，只显示指纹不泄密
- 用户数据全部迁入 userData，安装目录只读；日志 2MiB 轮转

### 🎵 音色训练（配套工具）

- 音色训练懒人包：普通版（轻量脚本）/ 完整版（自包含 + 预置苏苏洛音色免训练直接用）/ 苏苏洛音色独立部署包
- 显卡自动适配（按显存选批大小，无 N 卡自动 CPU 模式）

---

## 2.0 修复（2026-08-24）

- **Spine 姿势限框**：横姿/大姿势在结构 bounds 阶段提前 containment，避免真实内容被渲染裁切后再补救（上一版二次缩放在已裁切像素上无法生效）；`visibleCanvasGap` 仅用于地面间隙
- **手动 boost 姿势保持原尺寸**：仅当结构包围盒明显超出 120×120 画布时，按宽高比例临时收缩当前姿势，防止普通姿势因 metadata 空白被过度缩小

---

## v1.1.0

- 基础版：可配置 API、人设、情绪、本地语音（日语语音模式、中英日界面）
