# 依赖与构建产物
node_modules/
dist/

# 运行数据
data/
*.log

# 系统文件
.DS_Store
Thumbs.db

# 运行时备份（首次打开表情管理时自动生成）
renderer/sprites/default/

# 本地环境
.env
.env.local
renderer/spine/user/*
!renderer/spine/user/README-把模型放这里.txt
.watchdog-notes.md
__pycache__/
*.pyc

# 随包声音源（大模型文件，不入库）
voice/

# 发布产物 / 调试输出 / 备份目录（大文件不入库）
release/
outputs/
_backups/

# 内部交接与过程文档（含绝对路径/安全测试执行细节，不入公开仓库；本地保留供交接）
docs/internal/
docs/assets/

# Live2D Cubism Core（Live2D 专有许可：应用内捆绑合规，git 仓库不再分发）
renderer/live2dcubismcore.min.js
tests/__visual-out__/
