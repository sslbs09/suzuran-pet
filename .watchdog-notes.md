# 看门狗备忘（苏苏洛桌宠夜间任务）
用户已睡觉，授权自主完成。全部做完后：删除本文件、用 CronList 找到
「看门狗」自动化并 CronDelete 删除、简短汇报即可。禁止推送 GitHub。

## 已完成
- 音频已静音（config tts.enabled=false），引擎进程已停——不要再开
- 图层分级（托盘🗂 置顶/桌面）+ 📍坐任务栏托盘项已实现并部署

## 剩余步骤
1. main.js walkTick() 开头加 `if (walk.seated) return;`（坐下不移动）
2. main.js walkOnPhaseEnd() 开头加 seated 分支：defer 相位（stay seated）
3. main.js sitOnTaskbar()：置位 walk.seated=true 后调 walkBroadcast()
4. main.js 走路启动 startWalkingEngine() 里 reset walk.seated=false；
   「pet:walking-pause」释放分支末尾调 dragSeatUpdate() 定格座位状态
5. renderer/pet.js applyWalkState() 开头加 seated 分支：播放 Sit 循环
   （在 !walkState.active 判断之前）；init 回退 payload 加 seated:false
6. 部署 main.js+renderer/pet.js 到
   C:\Users\xsbil\Desktop\苏苏洛桌宠-1.1.0正式版\resources\app\ 并重启验证
7. 设置页 UI 重设计：重写 renderer/settings.css（保持所有类名/id 不变，
   用中性灰阶+蓝色主色卡片风；参考 Refactoring UI 色板），微调
   settings.html 结构允许的范围；main.js openSettings 窗口尺寸可调大些
8. npx eslint 全绿后 cp 到桌面版重启验证，git add -A 提交（不推送！）
